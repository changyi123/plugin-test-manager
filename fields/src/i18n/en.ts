import { Key } from './types';

const en: {
  [key in Key]: string;
} = {
  step: 'Step',
  expected: 'Expected',
  data: 'Data',
  precondition: 'Precondition',
  dataHasRemove: 'Related data has been removed',
};

export default en;
